## GitHub Actions disabled

This fork intentionally carries no active GitHub Actions workflow files.

When syncing or migrating to newer upstream `reth-bsc` versions, re-check this directory and remove or re-disable any reintroduced `*.yml` or `*.yaml` workflow files before pushing.

If repository-level GitHub Actions settings are also disabled, keep this file anyway so the intent remains visible in git history.
